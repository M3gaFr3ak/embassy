[target.'cfg(all(target_arch = "arm", target_os = "none"))']
# replace STM32L476RGTx with your chip as listed in `probe-rs chip list`
#runner = "probe-rs run --chip STM32L476RGTx --connect-under-reset"

[build]
target = "thumbv7em-none-eabi"

[env]
DEFMT_LOG = "trace"
